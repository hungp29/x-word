internal (x-word) has been deployed (gRPC).

For local access, port-forward the gRPC service:
  kubectl port-forward svc/{{ include "x-word.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }} -n {{ .Release.Namespace }}

Then use a gRPC client (e.g. grpcurl) against localhost:{{ .Values.service.port }}
